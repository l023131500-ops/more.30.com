/**
 * הכתובת הישנה, שנשמרת כדי שלא לשבור כתובת שכבר נמסרה.
 *
 * כל הטיפול עבר לנקודת הקצה האחת ב-/api/nedarim/webhook, וכאן
 * נשארת רק הפניה אליה. אין כאן לוגיקה משלה, ולכן אין סיכון
 * ששתי הכתובות יתחילו להתנהג אחרת זו מזו.
 */
export { POST, GET } from '../webhook/route';

export const dynamic = 'force-dynamic';
