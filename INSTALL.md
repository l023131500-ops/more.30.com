# התקנה והפעלה

עותק עצמאי ומלא של איגוד השיעורים. כל הקוד כאן, בשורש, בלי תלות
בריפוזיטורי אחר ובלי קוד של פרויקטים אחרים.

המסמך מוביל משתי נקודות התחלה אפשריות: חיבור למסד הקיים והפעלה תוך
דקות, או הקמה מאפס על פרויקט Supabase חדש.

---

## מה צריך

| רכיב | גרסה | הערה |
|---|---|---|
| Node.js | 20 ומעלה | נבדק על 24 |
| npm | 10 ומעלה | מגיע עם Node |
| חשבון Supabase | — | לפרויקט קיים או חדש |
| חשבון Vercel | — | רק לפריסה. פיתוח מקומי לא צריך |

---

## מסלול א: חיבור למסד הקיים

הדרך המהירה. המערכת עולה עם כל הנתונים שכבר במאגר.

```bash
npm install
npm run dev
```

זהו. האתר עולה על `http://localhost:3000`.

הפרויקט מכיל ברירות מחדל של כתובת Supabase ומפתח פרסום, ולכן הוא
עובד מיד בלי קובץ סביבה. **המפתח הזה אינו סוד** — הוא נשלח ממילא לכל
דפדפן שנכנס לאתר, וכל ההגנה נשענת על RLS ועל חשיפת עמודות מצומצמת
במסד. אין כאן דליפה.

להצבעה על פרויקט אחר, יוצרים `.env.local`:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://<הפרויקט>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<מפתח הפרסום>
```

---

## מסלול ב: הקמה מאפס על פרויקט חדש

### שלב 1: יצירת הפרויקט

יוצרים פרויקט Supabase חדש. מהגדרות ה-API לוקחים את הכתובת ואת מפתח
הפרסום, ומכניסים ל-`.env.local` כמו למעלה.

### שלב 2: הסכימה

הסכימה נבנתה בשורת מיגרציות. הדרך הפשוטה להעתיק אותה לפרויקט חדש היא
דרך Supabase CLI מול הפרויקט הקיים:

```bash
npx supabase db dump --db-url "<כתובת החיבור של הפרויקט הקיים>" \
  --schema public -f schema.sql
```

ואז להריץ את `schema.sql` על הפרויקט החדש, במסך ה-SQL בממשק.

הסכימה כוללת שלוש עשרה טבלאות בתחילית `igud_`, שתי תצוגות, RLS על
הכול, הרשאות ברמת עמודה לקורא אנונימי, ופונקציות הגשה אטומיות.

### שלב 3: חשבון מנהל

במסך ה-SQL של הפרויקט החדש:

```sql
select public.igud_create_login('you@example.com', 'סיסמה חזקה', 'שם מלא');
insert into public.igud_admins (user_id)
select id from auth.users where email = 'you@example.com';
```

### שלב 4: דלי המדיה

בממשק Supabase, ב-Storage, יוצרים דלי בשם `igud-media`, ציבורי,
מוגבל ל-5MB ולסוגי תמונה בלבד.

### שלב 5: נתונים ראשוניים

אין חובה. אפשר להתחיל ממאגר ריק ולהוסיף שיעורים דרך האתר, או לייבא
קובץ אקסל של נדרים פלוס במסך הניהול, בלשונית ייבוא וייצוא.

---

## פריסה

### Vercel

```bash
npx vercel
```

או דרך הממשק: מחברים את הריפוזיטורי, בוחרים את הענף, ופורסים.
Next.js מזוהה אוטומטית ואין צורך בהגדרות בנייה.

**חשוב:** אם הריפוזיטורי מכיל יותר מפרויקט אחד, יש להגדיר את
תיקיית השורש. בעותק העצמאי הזה השורש הוא השורש עצמו, ואין מה להגדיר.

### משתני סביבה

**המערכת עובדת במלואה בלי אף משתנה סביבה.** זה היה שיקול תכנוני:
כל מה שדורש הרשאות חורג עובר בפונקציות מסד צרות שבודקות הרשאה
בעצמן, ולא בחשבון-על שצריך להגדיר.

שני משתנים אופציונליים:

| משתנה | מה הוא נותן |
|---|---|
| `ANTHROPIC_API_KEY` | מפעיל את הסוכן הקולי החכם בשלוחה 5. בלעדיו השלוחה עובדת בחיפוש מילות מפתח |
| `NEXT_PUBLIC_SITE_URL` | כתובת מלאה לקישורים שהמערכת מייצרת. ברירת המחדל מספיקה ברוב המקרים |

---

## פקודות

| פקודה | מה היא עושה |
|---|---|
| `npm run dev` | שרת פיתוח עם רענון חם |
| `npm run build` | בנייה לייצור |
| `npm start` | הרצת הבנייה מקומית |
| `npx tsc --noEmit` | בדיקת טיפוסים בלבד |

---

## מפת הקוד

```
app/              דפי האתר ומסלולי ה-API
  admin/          מסך הניהול
  portal/         אזור אישי לרבנים ולמרכזים
  p/[token]/      עמוד אישי לפי קישור
  api/
    public/       ממשק קריאה פתוח
    yemot/        חמש שלוחות המערכת הקולית
    nedarim/      נקודת הקצה מול נדרים פלוס
components/       רכיבי הממשק
  admin/          מסכי הניהול
  form/           שדות הטפסים
lib/              לוגיקה משותפת
  supabase.ts     יצירת לקוחות המסד
  queries.ts      שאילתות ורשימות עמודות
  yemot.ts        בניית תשובות למערכת הקולית
  nedarim*.ts     קליטה ופרסום מול נדרים פלוס
  hebcal.js       לוח עברי, מימוש עצמאי
  xlsx.js         קריאת אקסל בדפדפן, בלי ספרייה חיצונית
docs/             התיעוד
scripts/          כלי ייבוא להרצה מקומית
public/brand/     הלוגו והנכסים הגרפיים
```

---

## התיעוד

| קובץ | מה בפנים |
|---|---|
| `docs/ARCHITECTURE.md` | מבנה המערכת והחלטות התכנון |
| `docs/YEMOT.md` | חיבור המערכת הקולית |
| `docs/YEMOT-BUILD.md` | **מסמך ביצוע להקמת הקו**, כולל כל התסריטים |
| `docs/NEDARIM.md` | החיבור לנדרים פלוס |
| `docs/HANDOFF.md` | מה בוצע ואיך ממשיכים |

---

## אבחון

| התסמין | הסיבה | הפתרון |
|---|---|---|
| האתר עולה אבל ריק משיעורים | המאגר ריק, או שכל השיעורים ממתינים לאישור | לאשר במסך הניהול, או לייבא נתונים |
| `Invalid API key` | כתובת או מפתח שגויים ב-`.env.local` | להשוות להגדרות ה-API בממשק Supabase |
| שגיאת הרשאה בבנייה | חסרים GRANT-ים על העמודות הציבוריות | להריץ מחדש את מיגרציית ההרשאות |
| שלוחות הקו לא עונות | הכתובת בקובצי ה-ini אינה נכונה | ראו `docs/YEMOT-BUILD.md`, סעיף אבחון |

---

## רישוי ושימוש

הקוד נכתב עבור איגוד השיעורים. הנתונים במאגר הם רשומות אמיתיות של
רבנים ובתי כנסת, וכוללים פרטי קשר. יש לטפל בהם בהתאם.
