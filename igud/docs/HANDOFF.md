# מה בוצע, ואיך ממשיכים

מסמך המשכיות. מי שפותח סשן חדש יכול לקרוא רק אותו ולהמשיך מהנקודה הזו.

---

## הכתובות

| מה | כתובת |
|---|---|
| האתר החי, מתעדכן בכל דחיפה לענף | https://igud-hashiurim-git-claude-igud-h-8be434-l023131500-ops-projects.vercel.app |
| הכתובת הקבועה, אחרי החלפת ענף הייצור | https://igud-hashiurim.vercel.app |
| מסך ניהול | /admin |
| אזור אישי | /portal |
| תיעוד ממשק | /api-docs |
| הקוד | ענף `claude/igud-hashiurim`, תיקייה `igud/` |
| מסד הנתונים | פרויקט Supabase `uhnrgujbdxhhmoxcjria` |

הכתובת הקבועה עדיין מצביעה על הבנייה הראשונה. כדי להעביר אותה לענף
העבודה: Vercel, פרויקט igud-hashiurim, Settings, Git, Production Branch,
בוחרים `claude/igud-hashiurim` ושומרים. מאותו רגע כל דחיפה לענף
מתפרסמת בכתובת הקבועה.

### כניסה למסך הניהול

שם משתמש `l023131500@gmail.com` או `0533116358`, סיסמה `EUEU1234`.
מומלץ להחליף את הסיסמה מיד, במסך הניהול או ב-Supabase.

---

## מה כבר עובד

### מסד הנתונים
מלא ופעיל ב-Supabase, פרויקט `uhnrgujbdxhhmoxcjria`.
12 טבלאות בתחילית `igud_`, שתי תצוגות, RLS על הכול, הרשאות ברמת עמודה
לקורא אנונימי, ופונקציות הגשה אטומיות. חשבונות המנהל נוצרו.

### נתונים אמיתיים
כל ארבעת הטפסים יובאו במלואם. אלה רשומות אמיתיות, לא נתוני הדגמה:

| הטופס | מה נכנס |
|---|---|
| 4320 | 22 שיעורים מפורסמים, 6 בתור האישור, 72 מועדים, 26 מגידי שיעור, 27 מקומות |
| 4063 | 19 בקשות לפתיחת שיעור |
| 4018 | 52 בקשות הצטרפות כמגיד שיעור |
| 4357 | 7 נרשמים לעדכונים |

הטקסונומיה: 244 ערכי בחירה ב-28 קבוצות, מכל ארבעת הטפסים.

לכל רשומה נשמר המזהה שלה בנדרים פלוס, ולכן סנכרון עתידי מעדכן אותה
במקום ליצור כפילות.

### האתר
דשבורד עם שני טורי כרטיסים וטור אנימציה של השיעורים הקרובים, קרוסלת
מרכזי תורה, חיפוש חכם עם שישה סינונים וכפתור "עוד", דף שיעור מלא עם
הורדה כתמונה וכ-PDF, העתקה, שיתוף וייצוא ליומן, דפי רב ומקום, אשף
הוספת שיעור בשישה שלבים, שני טופסי ההצטרפות, אזור אישי, מערכת ניהול
בשתים עשרה לשוניות, וקישור אישי לכל רב ולכל מרכז.

### התממשקויות
חמש שלוחות למערכת הקולית עם בנייה אוטומטית, סוכן קולי, ממשק ציבורי
לקריאה ודף תיעוד.

מול נדרים פלוס יש שתי דרכים מקבילות, לפי התיעוד הרשמי שלהם:
**משיכה** של רשומות הטפסים (`Forms/Manage.aspx` עם `Action=GetJson`,
`MosadId` ו-`ApiPassword`), עם סמן שמביא בכל פעם רק את החדש; ו-**callback**
לדחיפה בזמן אמת. שתיהן מתלכדות על מזהה הרשומה, ולכן אינן מייצרות כפילות.
לצידן יש מסך "בדיקת שדות" שמאמת את מספור העמודות מול רשומה אמיתית.

---

## מה נשאר להשלים

| נושא | מה צריך | איפה |
|---|---|---|
| בניית השלוחות | מפתח ימות המשיח כבר שמור. נותר ללחוץ **בניית השלוחות** פעם אחת | `/admin`, חיבורים והגדרות |
| אימות שדות נדרים | ללחוץ **בדיקת שדות** בטופס 4320, לוודא שהתוויות והערכים תואמים, ואז **סנכרון כל הטפסים** | `/admin`, אותה לשונית |
| מפתח Claude | לסוכן הקולי. בלעדיו הוא עובד בהתאמת מילים | `/admin`, אותה לשונית |
| גישת שרת | `IGUD_SERVICE_EMAIL` ו-`IGUD_SERVICE_PASSWORD` בסביבת Vercel | הגדרות הפרויקט ב-Vercel |
| ענף ייצור ב-Vercel | להעביר את ענף הייצור ל-`claude/igud-hashiurim` | Vercel, Settings, Git |
| דומיין | לחבר דומיין קבוע במקום כתובת vercel.app | Vercel, Domains |

**המפתחות שנמסרו כבר שמורים במסד** (טבלת `igud_settings`, מסומנים כסודיים
ונקראים רק בהרשאת מנהל): מספר המערכת ומפתח ה-API של ימות המשיח, ומספר
המוסד, ה-`ApiPassword` וה-`ApiValid` של נדרים פלוס. נוצר גם סוד callback
אקראי. אפשר לראות ולערוך את כולם במסך ההגדרות.

מכיוון שהמפתחות נמסרו בצ'אט, כדאי לדעת שאפשר להחליף אותם בכל רגע:
בנדרים פלוס במסך **עוד ← מפתחות API**, ובימות המשיח בהגדרות המערכת.
אחרי החלפה מדביקים את החדש במסך ההגדרות ושומרים.

---

## המשך פיתוח: מה כדאי להוסיף

לפי סדר התועלת:

1. **התראות** לרב ולמרכז כששיעור אושר או נדחה, בדוא"ל או בהודעה קולית.
2. **חיפוש לפי קרבה גיאוגרפית**. השדות `geo_lat` ו-`geo_lng` כבר קיימים
   בטבלאות אך אינם ממולאים. הוספת גיאוקודינג בעת האישור תפתח חיפוש
   "שיעורים לידי".
3. **זמני היום** מתוך לוח עברי, כדי לתמוך בשיעורים שנקבעים לפי מנחה
   ושקיעה. יש כבר מימוש לוח עברי ב-`lib/hebcal.js`.
4. **עמוד שבועי להדפסה** לכל בית כנסת, לתלייה על לוח המודעות.
5. **סטטיסטיקות** למנהל: כמה נכנסים, מה מחפשים, אילו ערים חסרות שיעורים.

---

## איך ממשיכים בסשן חדש

פותחים סשן חדש על אותו ריפוזיטורי ואותו ענף, ומוסרים את ההודעה הבאה:

> ממשיך את פרויקט איגוד השיעורים. הכול נמצא בתיקייה `igud/` בענף
> `claude/igud-hashiurim`. קרא קודם את `igud/docs/HANDOFF.md`
> ואת `igud/docs/ARCHITECTURE.md`, ואז נמשיך.
> מסד הנתונים הוא פרויקט Supabase `uhnrgujbdxhhmoxcjria`, וכל הטבלאות
> בתחילית `igud_`. המשימה הבאה שלי היא: <כאן כותבים מה רוצים>

זה מספיק כדי שהסשן החדש יעלה על המסלול בלי לחזור על העבודה.

---

## דברים שכדאי לדעת לפני שנוגעים בקוד

- **הבנייה חייבת לעבור**: `cd igud && npm run build`. אין להסתמך רק על
  `npm run dev`, כי בדיקות הטיפוסים רצות רק בבנייה.
- **המרת טפסים** יושבת כולה ב-`lib/nedarim.js`. אותו קובץ משמש גם את
  הדפדפן וגם את סקריפטי המסוף. שינוי שם משפיע על שני המסלולים.
- **פרוטוקול ימות המשיח** נבנה במקום אחד בלבד, בפונקציה `read`
  שבקובץ `lib/yemot.ts`. אם ההקשות לא נקלטות, זה המקום להתאים.
- **אין למחוק כלום מימות המשיח**. הקוד כותב קבצים בלבד, במכוון,
  והכתיבה חסומה מחוץ לשלוחת הבסיס.
- **נתונים אישיים** אינם נשמרים בריפוזיטורי. קובצי האקסל המקוריים
  לא הועלו, וחולצה מהם רק הטקסונומיה.
